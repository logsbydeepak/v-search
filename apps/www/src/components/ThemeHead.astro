<script is:inline>
  (() => {
    const saved = localStorage.getItem("quickly-theme");
    const preferred = window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
    document.documentElement.classList.toggle("dark", (saved || preferred) === "dark");
  })();
</script>
